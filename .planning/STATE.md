# Project State

## Work Completed

| Phase | Description                                                                                                                                                 | Architecture                                            | Ops                                                                                                 |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| 01    | Configured `pixi` and established Go/Node.js/Python structure.                                                                                              | [Architecture](documents/core/architecture.md)          | [Operations](documents/core/operations.md)                                                          |
| 02    | Integrated `vllm-mlx`, deployed `launchd` daemon, and handled zero-day config.json hot-patching.                                                            | [Architecture](documents/vllm-mlx/architecture.md)      | [Operations](documents/vllm-mlx/operations.md)                                                      |
| 03    | Seamless pivot to isolated Conda-Forge `ollama` daemon for native `gemma4` loading.                                                                         | [Architecture](documents/ollama/architecture.md)        | [Operations](documents/ollama/operations.md)                                                        |
| 04    | Executed `py-benchmark`. Validated `gemma4:26b` (MoE) 4x speed superiority (~50 T/s) over `31b` with equivalent RAG/JSON zero-shot constraint adherence.    | [Benchmark](documents/ollama/benchmark.md)              | N/A                                                                                                 |
| 05    | Integrated LangChain Eval & Arize Phoenix trace tests. Concluded BOTH 26b and 31b possess 100% logic stability for official LangChain Tool Calling and RAG. | [Evaluations](documents/evaluation/eval_rag_agent.json) | [Langchain Eval](documents/ollama/operations.md#4-langchain-official-integration--agent-evaluation) |
| 06    | Integrated Vite/React with FastAPI `lab_api` and tested ReAct, Stateful, Tooling, and Needle-in-Haystack RAG workflows.                                     | [Lab Architecture](documents/langchain/architecture.md) | [Lab Operations](documents/langchain/operations.md)                                                 |
| 07    | Enterprise Auto-Coding Agent: Implemented checking/RAG context inside native LangGraph cycle.                                                               | N/A                                                     | N/A                                                                                                 |
| 08    | Standalone General ReAct Agent & File System Sandbox Agent architecture implemented.                                                                        | N/A                                                     | N/A                                                                                                 |
| 10    | E-Commerce GraphRAG Assistant (Mock Text-to-Cypher): Implemented UI, 100K Mass Data Generator, and Dynamic Vocabulary Mapping via `GraphCypherQAChain`.     | [GraphRAG](documents/graphrag-ecommerce/architecture.md)| [Ops](documents/graphrag-ecommerce/operations.md)                                                                 |
| 12/13 | Dual-Database Retail Infrastructure & Hybrid Synthesis: Postgres ↔ Neo4j ETL, Gemini 2.5 Flash extraction, Checkpoint Resumption, Schema Constraints, and real-world Customer/Order/Review graph injection. | [Plan](.planning/phases/12-retail-data-infra/PLAN.md) | [Ops](documents/graphrag-ecommerce/operations.md) |
| 14    | Tailwind Interactive UI Documentation Redesign: Transformed generic dashboard interfaces into a high-fidelity documentation theme mimicking Official Tailwind aesthetics. | [Plan](.planning/phases/14-tailwind-docs/PLAN.md) | N/A |
| 15    | Enterprise AutoCoder (React Lab UI): Ported the Phase 09 Autonomous Agent into a live-updating React frontend with SSE streaming and dynamic configuration.                 | [Plan](.planning/phases/15-enterprise-autocoder/PLAN.md) | [Ops](documents/langgraph/operations.md) |
| 16    | Langfuse Observability Integration: Transitioned default system telemetry to Langfuse SDK v3 locally with Docker Compose, securing parameter injection and implementing fault-tolerant initialization logic.      | [Architecture](documents/langfuse/architecture.md) | [Ops](documents/langfuse/operations.md) |
| 17    | E-Commerce GraphRAG Langfuse Observability: Instrumented the E-Commerce backend with LangChain tracing for both Neo4j extraction and conversational API endpoints, increasing retrieval depth (`k=1000`) to resolve synthetic duplication. | [Plan](.planning/phases/17-ecommerce-langfuse/PLAN.md) | [Ops](documents/langfuse/operations.md) |

## Current Work in Progress

- **None**

## Quick Tasks Completed

| Task ID  | Description                                                                                                                                                                                                       |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| quick-15 | Fixed API FastApi 429 Rate Limit issues in ETL (`async_fetch.py`) by limiting `Semaphore(3)` and increasing exponential backoff, ensuring the 100k extraction stabilizes under Gemini's Free Tier quotas.       |
| quick-14 | Fixed LangChain `Neo4jVector.from_existing_index` validation error by dropping the invalid `text_node_properties` kwarg, resolving minutes-long FastAPI boot hangs.                                                 |
| quick-13 | Validated E2E GraphRAG extraction and implicit node hops via Next.js React UI (`page.tsx`), successfully rendering `Knowledge Graph` metadata pills on right-side inspector.                                      |
| quick-12 | Executed comprehensive E2E Verification Script (`graphrag-verify`) confirming 9 metrics including 104k dataset volume matching across Neo4j and Postgres via `hybrid_amazon_generator.py`.                      |
| quick-11 | Integrated Split-Panel Design into Frontend. Stripped redundant product parsing text from Chat bubbles, routing metadata clearly to the dedicated Product Cards panel.                                              |
| quick-01 | Updated `.agent/rules/rules.md` to default Web/CSS frameworks to HTML+Tailwind instead of Vanilla CSS.                                                                                                            |
| quick-02 | Fixed `pixi.toml` parsing error by restoring `[system-requirements]` as a global table. Pixi does not support target-specific system-requirements.                                                                |
| quick-03 | Resolved `pixi install` conflict by bumping `pillow>=10.0.0` and isolating `vllm-mlx` solely to the `osx-arm64` platform string.                                                                                  |
| quick-04 | Fixed MLX PyPI wheel resolution parsing by setting globally `macos = "14.0"` in system-requirements to match modern Apple Silicon wheel deployments.                                                              |
| quick-05 | Segregated `pillow>=10.0.0` strictly into `[feature.main.target.osx-arm64.dependencies]` while restoring global Linux `pillow` to `*` to bypass `libjpeg-turbo` conda-forge strict priority constraints on Linux. |
| quick-06 | Hot-patched HuggingFace cache config.json to downgrade `gemma4` zero-day structural flags to `gemma3` flags, bypassing `mlx_lm` validation constraints safely.                                                    |
| quick-07 | Added `ollama-list` to `pixi.toml` and updated `operations.md` to quickly view downloaded models.                                                                                                                 |
| quick-08 | Added `eval-langchain` to `pixi.toml` dependencies and operations.md via GSD (Get Shit Done) strict workflow. Tested LangChain bind_tools and Needle-in-Haystack RAG tests effectively.                           |
| quick-09 | Updated `.agent/rules/rules.md` and `strict-gsd.md` with explicit policies requiring the shutdown of background service testing CLIs.                                                                             |
| quick-10 | Moved the root `backend/` Python files into `backend/ecommerce-graphrag/` to properly namespace the microservices. Updated `Makefile` and `cli.py` relative `.env` load paths to reflect the new structure.     |
| quick-16 | Added missing `langchain-ollama` dependency to `pixi.toml` to fix `ModuleNotFoundError` during Phase 15 Enterprise AutoCoder execution.                                                                             |
| quick-17 | Fixed `UnboundLocalError` for `os` module in `main.py` by scoping out redundant inner `import os` statements inside `_run()` that shadowed global namespace.                                                      |
| quick-18 | Fixed MCP file access denial on macOS by resolving symlinks via `os.path.realpath()` for all sandbox parameters, bridging the `/tmp` to `/private/tmp` validation gap.                                        |
| quick-19 | Registered both the original string and the realpath to MCP allowed paths array, and truncated the persistent thread ID sandbox dir to 8 characters to prevent LLM string-generation stutter failure mode.      |
| quick-20 | Fixed infinite polling loop where Agent was instructed to `list_directory` on empty `/tmp/autocoder_` directory by properly filtering `/tmp` paths from the `work_targets` prompt variable.                     |
| quick-21 | Split allowed_dirs into two layers: `_project_dirs` (prompt-visible, realpath-resolved, no /tmp) vs raw `allowed_dirs` (MCP backend permissions). Eliminates sandbox dir leaking into agent prompts.             |
| quick-22 | Fixed `NameError: new_dir` in `main.py:340` — variable was renamed in quick-19 refactor but print statement still referenced old name. Changed to `real_dir`. |
| quick-23 | Applied `os.path.realpath()` to all dirs in `mcp_proxy.py` before passing to MCP filesystem server args, ensuring canonical `/private/tmp` form on macOS. |
| quick-24 | Removed sandbox `/tmp/autocoder_*` dir injection from `all_extra_dirs` entirely. Agent works directly on project dirs — sandbox polluted MCP tool descriptions and caused model to loop on empty directories. |
| quick-25 | Added no-op detection in `test_node`: catches coder sub-agent "I cannot proceed" give-up responses and file existence validation against the plan. Prevents false-positive test passes on unchanged code. |
| quick-26 | Fixed path extraction regex to support backtick/quote-wrapped paths (`` `/path/to/dir` ``). Previous regex required whitespace before `/`, silently failing on markdown-formatted prompts → empty allowed dirs. |
| quick-27 | Added `--dir` / `-d` CLI flag to `autonomous` command as primary, explicit directory source. Merged with legacy `--work-dir`. All CLI dirs are realpath-resolved. Eliminates regex-only dependency for path discovery. |
| quick-28 | **Langfuse SDK v3 Session Hotfix**: Removed deprecated `CallbackHandler(session_id=thread_id)` constructor parameter from both `run()` and `astream_run()`. Migrated to `config.setdefault("metadata", {})["langfuse_session_id"] = thread_id` per official Langfuse v3 docs. |
