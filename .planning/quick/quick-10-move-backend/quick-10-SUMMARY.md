## Quick Summary: quick-10-move-backend

Moved the root `backend/` Python files into `backend/ecommerce-graphrag/` to properly namespace the microservices. Updated `Makefile` and `cli.py` relative `.env` load paths to reflect the new structure.
